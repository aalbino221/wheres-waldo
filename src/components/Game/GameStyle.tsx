import styled from 'styled-components';

const GameDiv = styled.div`
  height: fit-content;
  width: 90vw;
  display: flex;
  margin: 0 auto;

  & > img {
    width: 100%;
  }
`;

export default GameDiv;
